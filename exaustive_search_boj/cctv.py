# BOJ 15683
import sys

si = sys.stdin.readline

"""
def dfs(x, y, direction):
    if graph[x][y] == 0:
        graph[x][y] = -1
    nx = x + direction[0]
    ny = y + direction[1]
    if 0 <= nx and nx < n and 0 <= ny and ny < m:
        if graph[x][y] != -1 and graph[x][y] != 6:
            dfs(nx, ny, direction)
"""